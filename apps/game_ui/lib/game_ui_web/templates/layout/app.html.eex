<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>GameUi · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>"/>
  </head>
  <body>
    <header>
      <section class="container header-section">
        <h1 class="header-title">
          <span>#</span> <span>TIC</span> <span>TAC</span> <span>TOE</span></h1>

        <div class="user-info">
          <%= if @conn.assigns.current_player do %>
            <%= link "X", to: player_path(@conn, :delete, @conn.assigns.current_player), class: "close", method: :delete %>
          <% end %>
        </div>
      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= render @view_module, @view_template, assigns %>
    </main>
    <script>window.currentPlayer = "<%= @conn.assigns.current_player %>"</script>
    <script type="text/javascript" src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
